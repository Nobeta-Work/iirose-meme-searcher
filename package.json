{
  "name": "iirose-meme-searcher",
  "version": "0.1.0",
  "private": true,
  "description": "iirose web plugin for meme search and quick send.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "relay:bing": "node scripts/bing-relay.mjs",
    "print:installer": "node scripts/print-installer.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.2",
    "playwright-core": "^1.58.2"
  }
}
